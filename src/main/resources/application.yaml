debug: false # ??? ?? ????
management.endpoints.web.exposure.include: "*" #actuator? endpoint? ?? ? ? ??

logging:
  level:
    com.fastcampus.projectboard: debug
    org.springframework.web.servlet: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace # ?? ??? debug ??? ?? ????

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board
    username: 4shchoi4
    password: passwd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    defer-datasource-initialization: true # ???? db ??
    hibernate.ddl-auto: create #entity? ?? ???? ddl? ?????.
    show-sql: true #????? sql ??? ????.
    properties:
      hibernate.format_sql: true #??? ?? ?? ????? ????. ??? ?? hibernate ??
      hibernate.default_batch_fetch_size: 100 #?? 100? ??? ??? select ? ? ??? ??.
  sql.init.mode: always
  data.rest:
    base-path: /api
    detection-strategy: annotated
# ?? document

#  datasource:
#    url: jdbc:h2:mem:board;mode=mysql
#    driverClassName: org.h2.Driver
#  sql.init.mode: always
#  test.database.replace: none
